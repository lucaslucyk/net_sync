import React, { Component } from "react";

export default class CredentialListPage extends Component {
  constructor(props){
    super(props);
  }

  render() {
    return <p>This is Credential List Page.</p>
  }
}